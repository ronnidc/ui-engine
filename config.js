// Base-path used during development while building prototypes:
export const BASE_PATH = '/';

// Base-path used when exporting the prototype with `npm run generate`:
// eg. = '/poc/test7maj';
export const PREVIEW_BASE_PATH = '/';
